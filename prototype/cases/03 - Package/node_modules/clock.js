// notice: we are exporting regular common js module
module.exports = Clock;

function Clock() {
  var dom = this.initDom();

  setInterval(function () {
      var now = new Date();
      dom.hours = now.getHours();
      dom.minutes = now.getMinutes();
      dom.seconds = now.getSeconds();
    }, 1000);
}
